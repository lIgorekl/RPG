namespace Gameplay.AI
{
    // Интерфейс политики агрессии врага
    // Определяет, может ли враг атаковать игрока
    // в текущих условиях игры
    public interface IAggroPolicy
    {
        // Возвращает true, если врагу разрешено
        // переходить в агрессивное поведение
        bool CanAggro();
    }
}